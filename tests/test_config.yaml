metadata:
  sample_annotation: samples.csv
  output_dir: test
  pipelines_dir: pipelines
  merge_table: merge.csv

derived_columns: [file, file2, dcol1, dcol2]

data_sources:
  src1: "$CODEBASE/looper/tests/data/{sample_name}{col_modifier}.txt"
  src3: "$CODEBASE/looper/tests/data/{sample_name}.txt"
  src2: "$CODEBASE/looper/tests/data/{sample_name}-bamfile.bam"
