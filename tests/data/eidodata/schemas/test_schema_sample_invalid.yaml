description: test PEP schema

properties:
  dcc:
    type: object
    properties:
      compute_packages:
        type: object
  samples:
    type: array
    items:
      type: object
      properties:
        sample_name:
          type: string
        protocol:
          type: string
        genome:
          type: string
        newattr:
          type: string
      required:
        - newattr

required:
  - samples
